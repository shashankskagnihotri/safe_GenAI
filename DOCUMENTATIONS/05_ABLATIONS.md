# Ablations

`configs/experiments/ablations.yaml` defines local overrides for:

- no steering,
- binary/global-ish mask behavior,
- lower lambda,
- higher unsafe activation margin.

Run locally with:

```bash
hierasafe-grid --config configs/experiments/ablations.yaml
```

